<div class="row">
  <div class="col-lg-12">
  <header class="panel-heading" >Reportes de ayuda</header>
    <section class="panel">
      <div class="panel-body">
        <div class="table-responsive">
          <table class="table">
            <thead>
              <tr>
                <th>Causa</th>
                <th>Email</th> 
                <th>Teléfono</th>
                <th>Opciones</th>
              </tr>
            </thead>
            <tbody>
              <% @tikets.each do |ticket| %>
              <tr>
                <td><%= ticket.cause%></td>
                <td><%= ticket.email%></td>
                <td><%= ticket.phone%></td>
                <td><%= link_to 'Abrir', ticket_path(ticket.id)%></td>
              </tr>
              <%end%>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </section>
</div>