<div class="row">
  <div class="col-lg-12">
  <header class="panel-heading" >Roles</header>
  <section class="panel">
    <div class="panel-body">
      <%= link_to 'Nuevo Rol', new_role_path, class: 'btn btn-default'%>
      <div class="table-responsive">
        <table class="table">
          <thead>
            <tr>
              <th>Nombre del Rol</th>
              <th>Esta activo</th>
              <th>Opcciones</th>
            </tr>
          </thead>
          <tbody>
            <% @roles.each do |r|%>
            <tr>
              <td><%= r.title %></td>
              <td><%= r.active_role %></td>
              <td><%= link_to 'Eliminar', destroy_role_path(id: r.id), "data-confirm": "¿Estas seguro de querer eliminar este rol?" %></td>
            </tr>
            <%end%>
          </tbody>
        </table>
      </div>
    </div>
  </section>
  </div>
</div>